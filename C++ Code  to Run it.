Assuming that you are using a microcontroller or a development board to control your buck converter and linear voltage regulator, here's a general outline of how you might structure your code in both C++ and Python:
#include <iostream>
// Define pin configurations
const int buckConverterPin = 9;
const int voltageRegulatorPin = 5;

// Function to set the output voltage of the buck converter
void setBuckConverterVoltage(int voltage) {
    // Your implementation to control the buck converter
    // You might use libraries or specific instructions based on your hardware
    // Example: some_library.setVoltage(buckConverterPin, voltage);
}

// Function to set the output voltage of the linear voltage regulator
void setVoltageRegulatorVoltage(int voltage) {
    // Your implementation to control the linear voltage regulator
    // Example: another_library.setVoltage(voltageRegulatorPin, voltage);
}

int main() {
    // Initialize hardware, if needed

    // Set the output voltage of the buck converter
    setBuckConverterVoltage(12);

    // Set the output voltage of the linear voltage regulator
    setVoltageRegulatorVoltage(3.3);

    // Your main control loop or other logic

    return 0;
}
